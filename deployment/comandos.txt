#1
docker run --name docker-postgres -e POSTGRES_PASSWORD=mysecretpassword -d postgres -p 5433:5432

#2
psql postgres postgres
mysecretpassword

#3
CREATE TABLE TICKET(ID SERIAL PRIMARY KEY, USUARIO VARCHAR(255), FECHA_CREACION TIMESTAMP, FECHA_ACTUALIZACION TIMESTAMP, ES_ABIERTO BOOLEAN);

#4
INSERT INTO TICKET (USUARIO, FECHA_CREACION, FECHA_ACTUALIZACION  , ES_ABIERTO ) VALUES 
	('Alejandra Cifuentes', CURRENT_TIMESTAMP, NULL, TRUE),
	('Deysi Perez', CURRENT_TIMESTAMP, CURRENT_TIMESTAMP, FALSE),
	('Javier Suarez', CURRENT_TIMESTAMP, NULL, FALSE);